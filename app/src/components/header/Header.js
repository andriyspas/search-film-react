import React from 'react';
import Logo from '../../images/logo.png';

const Header = () =>
    <header>
        <a href="/" >
            <img src={ Logo } />
        </a>
    </header>;

export default Header;
